# ─── Серверный пайплайн (transcriber.py) ───
requests>=2.31          # HTTP к speech2text + LLM API
python-docx>=1.1        # генерация .docx

# ─── FastAPI-приёмник аудио (api.py) ───
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9 # приём файлов (multipart/form-data)

# ─── Telegram-бот (bot.py) ───
python-telegram-bot>=21.0
httpx>=0.27             # HTTP-клиент с поддержкой прокси

# Десктоп-клиент (desktop/) ставится отдельно: PyQt6 + requests
